# Base de Dados

Esta pasta contém a base de dados e os scripts utilizados pelo Assistente de Transporte com IA.

## Arquivos

- `transporte.db` — banco de dados SQLite utilizado pelo projeto.
- `schema.sql` — estrutura SQL utilizada para criação das tabelas.
